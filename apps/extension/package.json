{
  "name": "@onda/extension",
  "version": "0.1.0",
  "private": true,
  "description": "onda — detect what you're listening to, send gifts directly to artists",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "viem": "^2.47.6"
  }
}
